var inquirer = require("inquirer");

var Letter = require("./letter");
var Word = require("./word");


// Set wordArr to contain an array words.


// A function that starts the game. Randomizies the array to pick a word for the user to guess. Then calls charChecker() from letter to check to see if the user made a correct or incorrect guess. Shows letters choosen and if shows the letter in the word if correct.

//a function that ends the game. Checks if the user has answered the letter correctly using correctGuess() and if this shows to be true we call displayWord() to display the word and console.log "You won". If not correct and guesses run out the console.log "You lose" and call start game function to restart game. 




